<%- include('partials/recentes.ejs')%>
    <div class="row">
        <% if (itensCont) { %>
            <div class="col-9">
                <%- include('user/conteudo.ejs')%>
            </div>
            <% } else { %>

            <% } %>
                    <% if (destaques) { %>
                        <div class="col-3">
                            <%- include('user/maisbaixados.ejs')%>
                        </div>
                        <% } else { %>

                            <% } %>

    </div>